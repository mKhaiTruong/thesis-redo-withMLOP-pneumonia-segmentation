[project]
name = "pnseg-orchestrator"
version = "0.1.0"
dependencies = [
    "pnseg-core",
    "httpx",
    "docker",
    "docker-composer",
    "prefect",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["orchestrator*"]