[project]
name = "pnseg-ingestion"
version = "0.1.0"
dependencies = [
    "pnseg-core",
    "kaggle",
    "python-dotenv",
    "joblib",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["ingestion*"]