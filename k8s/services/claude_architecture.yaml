apiVersion: apps/v1
kind: Deployment
metadata:
  name: claude-architecture
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: claude-architecture
  template:
    metadata:
      labels:
        app: claude-architecture
    spec:
      containers:
        - name: claude-architecture
          image: bill123mk/pneumonia_mlops:claude-architecture
          imagePullPolicy: Always
          ports:
            - containerPort: 7860
          envFrom:
            - secretRef:
                name: app-secrets
          volumeMounts:
            - name: artifacts
              mountPath: /app/artifacts
      volumes:
        - name: artifacts
          persistentVolumeClaim:
            claimName: artifacts-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: claude-architecture
spec:
  selector:
    app: claude-architecture
  ports:
    - port: 7860
      targetPort: 7860
  type: ClusterIP
