[project]
name = "pnseg-orchestrator"
version = "0.1.0"
dependencies = [
    "pnseg-core",
    "httpx",
    "docker",
    "prefect",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["orchestrator*"]