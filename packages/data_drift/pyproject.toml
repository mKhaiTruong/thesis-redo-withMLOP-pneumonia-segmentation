[project]
name = "pnseg-data_drift"
version = "0.1.0"
dependencies = [
    "pnseg-core",
    "numpy",
    "mlflow",
    "huggingface_hub",
    "python-dotenv",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["data_drift*"]