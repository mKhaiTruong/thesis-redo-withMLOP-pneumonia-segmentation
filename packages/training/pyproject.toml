[project]
name = "pnseg-training"
version = "0.1.0"
dependencies = [
    "pnseg-core",
    "torch",
    "segmentation-models-pytorch",
    "mlflow",
    "albumentations",
]

[tool.setuptools.packages.find]
where = ["src"]